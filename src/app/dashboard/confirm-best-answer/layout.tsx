"use client";

export interface IDashboarLayoutProps {
  children: React.ReactNode;
}

const NewQuestionLayout = (props: IDashboarLayoutProps) => {
  return props.children;
};

export default NewQuestionLayout;
